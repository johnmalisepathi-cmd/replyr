{
  "name": "art-generator-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal proxy API that calls OpenAI's image generation API, keeping the API key server-side.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
